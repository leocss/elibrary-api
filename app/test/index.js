var chai = require('chai');

describe('Authentication', function() {
    it('Should return an access token if details are valid', function() {

    });
});

describe('Resources', function() {
    it ('Should return error response if no access_token is specified.', function() {

    });

    it ('Should return a book resource', function() {

    });
});